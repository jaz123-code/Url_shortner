version: '3.9'

services: 
    app:
      build: .
      container_name: url_shortener_app
      ports:
         - "8000:8000"
      depends_on:
         - redis
    redis:
      image: redis:7
      container_name: url_shortener_redis
      ports:
        - "6379:6379"